# BuyWise frontend API connection (Phase C).
# Set the deployed API Gateway invoke URL here for local builds, and in
# AWS Amplify Console -> App settings -> Environment variables for the
# deployed app. Trailing slashes and a missing /recommend path are handled.
# Leave empty to keep the frontend fully offline (local engine).
VITE_BUYWISE_API_URL=
